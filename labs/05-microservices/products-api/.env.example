# Server
PORT=3001
API_VERSION=1.0.0

# Auth — set to "none" to disable auth (dev only)
AUTH_MODE=apikey
API_KEY=changeme

# Database — shared Postgres instance
DATABASE_URL=postgresql://api_user:api_pass@localhost:5432/shared_db

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100
